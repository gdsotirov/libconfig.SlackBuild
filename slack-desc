libconfig: libconfig (library for processing structured cofiguration files)
libconfig:
libconfig: Libconfig is a simple library for processing structured
libconfig: configuration files. The file format is more compact and more
libconfig: readable than XML. And unlike XML, it is type-aware, so it is
libconfig: not necessary to do string parsing in application code.
libconfig: Libconfig is very compact - a fraction of the size of the expat
libconfig: XML parser library. This makes it well-suited for
libconfig: memory-constrained systems like handheld devices.
libconfig: The library includes bindings for both the C and C++ languages.
libconfig: It works on POSIX-compliant UNIX systems (GNU/Linux, Mac OS X,
libconfig: Solaris, FreeBSD) and Windows (2000, XP and later).
libconfig:
libconfig: Packaged by Georgi D. Sotirov <gdsotirov@dir.bg>
